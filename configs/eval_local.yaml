# Local sim-to-sim comparison on this box: CPU sims + the nexus (GPU) backend.
robot: robot/lerobot_legs.yaml
policy: policy/lerobot_legs_flat.yaml
sims: [mujoco, pybullet, nexus]
episodes: 3
max_steps: 400
control_dt: 0.02
seeds: [0, 1, 2]
fall_height: 0.35
fall_tilt: 0.7
render: false
